externs = {
    'add': lambda a, b: a + b,
    'sub': lambda a, b: a - b,
    'mul': lambda a, b: a * b,
    'div': lambda a, b: a // b,
    'lt':  lambda a, b: a < b,
    'gt':  lambda a, b: a > b,
    'elt':  lambda a, b: a <= b,
    'egt':  lambda a, b: a >= b,
    'eq_comp': lambda a, b: a == b,
    'and': lambda a, b: a and b,
    'or': lambda a, b: a or b,
}