#import BasicAxioms
import MathAxioms

Hypothesis:
    let a be Nat = 3
    let AB = 3
    let number = 4
    let randomtuple : Namedtuple = (r = 4, g = 200, b = 250)
    let anontuple : Tuple = (120, 121, 330)


Proof:
    randomtuple's r equals number
    anontuple[0] equals 120