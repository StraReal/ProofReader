#import BasicAxioms
import MathAxioms

Hypothesis:
    let a be Int
    let b be Int = 2*a

Proof:
    let t = (isEven b with witness 2)
    ((2+3 equals 5) equals (isEven b with witness 2)) is true #correctly errors out, switch to "false" and it will work.
    t is false
