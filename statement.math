#import BasicAxioms
import MathAxioms

Hypothesis:
    let a be Tuple = (0,2,3)
    let s be string = "hello"

Proof:
    (1 into Bool) equals true
    (020 into Str) equals "20"
    (123 into Int) equals 123
    123 into Tuple