#import BasicAxioms
import MathAxioms

Hypothesis:
    let a : Nat = 5
    let b : Nat = 6
    let c : Nat = 2
    let d : Nat = 1
    let x : Int
    let ABC
    let DEF
    ABC = DEF
    let truevar : Bool = true

Proof:
    a is congruent to b - 1